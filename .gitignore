# Vendored upstream Gateway source (see README.md "Upstream baseline" for the
# pinned clone/checkout commands). Not committed here: it's a large third-party
# tree with its own git history, submodules, and build/install output.
rticonnextdds-gateway/

# Runtime-generated demo artifacts
demo/logs/
demo/config/shape_type.pbdesc

# OS/editor cruft
Thumbs.db
.DS_Store
